Servidor Web con configuración mínima que sirve como punto de Partida para
crear un backend desde cero en NodeJS

Instalar dependencias
```
npm install
```

Ejecutar Servidor
```
npm run dev
```